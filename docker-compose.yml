version: '3.8'

services:
  pulseapi-backend:
    build: .
    ports:
      - "8000:8000"
    environment:
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN:-}
    volumes:
      - ./server/pulseapi.db:/app/server/pulseapi.db
